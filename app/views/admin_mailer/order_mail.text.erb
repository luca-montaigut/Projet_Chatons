<%= @order.user.first_name %> <%= @order.user.last_name %> vient de passer une commande.

Voici un recapitulatif de la commande.

<% @order.items do |item| %>
- <%= item.title %>

<% end %>

Total de la commande: <%= @order.total %>€
